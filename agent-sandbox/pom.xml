<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.qwzhang01</groupId>
        <artifactId>seven-agent</artifactId>
        <version>0.1.5</version>
    </parent>

    <artifactId>agent-sandbox</artifactId>
    <name>Agent Sandbox</name>
    <description>Sandbox for isolated execution of LLM-generated code</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <environmentVariables>
                        <!-- Red-team fixture (ProcessSandboxRedTeamTest.RED-2):
                             a host-side secret the guest must NOT inherit. -->
                        <SANDBOX_REDTEAM_SECRET>host-secret-value</SANDBOX_REDTEAM_SECRET>
                    </environmentVariables>
                    <!-- Stage 8.3: surefire's default includes only match *Test
                         classes; DockerDaemonProbeIT uses the IT suffix. Keep
                         the defaults and add IT so the container profile is
                         discoverable (it runs by default alongside the
                         sandbox-escape suite, no external dependency is
                         required; absent docker degrades to an auditable
                         Assumptions skip). -->
                    <includes>
                        <include>**/Test*.java</include>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*TestCase.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>io.github.qwzhang01</groupId>
            <artifactId>agent-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
